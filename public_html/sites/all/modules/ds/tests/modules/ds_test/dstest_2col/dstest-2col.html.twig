{#
/**
 * @file
 * Display Suite 2 column template.
 *
 * Available variables:
 * - layout_wrapper: @todo
 * - left_wrapper: @todo
 * - right_wrapper: @todo
 * - attributes: @todo
 * - left_classes: @todo
 * - right_classes: @todo
 * - left: @todo
 * - right: @todo
 */
#}
<{{ layout_wrapper }} class="ds-2col {{ attributes.class }} clearfix" {{ attributes }}>

  {% if title_suffix.contextual_links is not null %}
    {{ title_suffix.contextual_links }}
  {% endif %}

  <{{ left_wrapper }} class="group-left{{ left_classes }}">
    {% if left is not null %}
      {{ left }}
    {% endif %}
  </{{ left_wrapper }}>

  <{{ right_wrapper }} class="group-right{{ right_classes }}">
    {% if right is not null %}
      {{ right }}
    {% endif %}
  </{{ right_wrapper }}>

</{{ layout_wrapper }}>

{% if drupal_render_children is not null %}
  {{ drupal_render_children }}
{% endif %}
